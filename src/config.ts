/**
 * List the repos you want to see notifications for.
 * Use the format "owner/repo".
 * Leave the array empty to show all repos.
 */
export const WATCHED_REPOS: string[] = [
  "websummit/attendee-portal",
  "websummit/emm",
  "admxxi/tickerly"
]
